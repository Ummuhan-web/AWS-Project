from flask import Flask, render_template, request

app = Falsk(__name__)

def converter(user_age):
    if user_age > 75 or cronic == 'yes' :
     return "You are in risky group"
    else :
     return "You are not in risky group"
